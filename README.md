# Live-css-update



The prototype function _add_style() adds  new css properties to the element and with _overwrite parameter overwrites the existing values.


Return Value:	A String, representing the text value
